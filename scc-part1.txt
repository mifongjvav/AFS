//Label插值
//Stringa
//Label到
//Stringb
//Label_FF0C_效果为
//Custom函数["渐陡n次","渐缓n次","三角函数","三角缓入","三角缓出","掉落弹跳"]
//Label_FF0C_参数
//Stringn
//Label_FF0C_进度
//Stringt
//Label_25_
function 函数_数_字符_1(a, b, 函数, n, t) {
  if (函数 === "渐陡n次") {
    函数返回(a + (b - a) * (t / 100 ^ n));
  } else if (函数 === "渐缓n次") {
    函数返回(a + (b - a) * (1 - (1 - t / 100 ^ n)));
  } else if (函数 === "三角函数") {
    函数返回(a + (b - a) * ((1 + 三角函数("sin", 360 * (n * (t / 100)) - 90)) / 2));
  } else if (函数 === "三角缓入") {
    函数返回(a + (b - a) * 三角函数("sin", 90 * (t / 100)));
  } else if (函数 === "三角缓出") {
    函数返回(a + (b - a) * (1 - 三角函数("cos", 90 * (t / 100))));
  } else if (函数 === "掉落弹跳") {
    函数返回(b + (a - b) * (科学计算("绝对值", 三角函数("sin", 180 * (列表值(分开字符串(n, ","), "第", 1) * (t / 100)))) * (2.718281828 ^ 列表值(分开字符串(n, ","), "第", 2) * (t / 100))));
  } else {
    函数返回(0);
  }
}
函数_数_字符_1(0, 100, "渐陡n次", 1, 0);